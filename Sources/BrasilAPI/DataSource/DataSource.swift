import Foundation

public enum DataSource {
    enum Bank { }
    enum Broker { }
    enum CNPJ { }
    enum CPTEC { }
    enum DDD { }
    enum Exchange { }
    enum FIPE { }
    enum Holidays { }
    enum ZipCode { }
    enum IBGE { }
    public enum ISBN { }
}
