import Foundation

public enum DataSource {
    public enum Bank { }
    public enum Broker { }
    public enum CNPJ { }
    public enum CPTEC { }
    public enum DDD { }
    public enum Exchange { }
    public enum FIPE { }
    public enum Holidays { }
    public enum IBGE { }
    public enum ISBN { }
    public enum NCM { }
    public enum PIX { }
    public enum ZipCode { }
}
