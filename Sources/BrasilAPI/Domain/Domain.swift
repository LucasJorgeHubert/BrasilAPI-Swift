import Foundation

public enum Domain {
    enum Bank { }
    enum Broker { }
    enum CNPJ { }
    enum CPTEC { }
    enum DDD { }
    enum Exchange { }
    enum FIPE { }
    enum Holidays { }
    enum ZipCode { }
    enum IBGE { }
    public enum ISBN {
        public enum Models { }
    }
}
