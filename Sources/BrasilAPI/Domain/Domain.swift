import Foundation

public enum Domain {
    enum Bank { }
    enum Broker { }
    enum CNPJ { }
    enum CPTEC { }
    enum DDD { }
    enum Exchange { }
    enum FIPE { }
    enum Holidays { }
    enum ZipCode { }
    public enum IBGE {
        public enum Models { }
    }
    public enum ISBN {
        public enum Models { }
    }
}
