import Foundation

public enum Presenter {
    public enum Bank { }
    public enum Broker { }
    public enum CNPJ { }
    public enum CPTEC { }
    public enum DDD { }
    public enum Exchange { }
    public enum FIPE { }
    public enum Holidays { }
    public enum ZipCode { }
    public enum IBGE { }
    public enum ISBN {
        public enum UseCase { }
    }
}
